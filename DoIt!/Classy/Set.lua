-- Initialize local namespace 
local addonName, addon = ...
local _G = _G

Set = {
    new = function (constructor)
        return addon.createInstance(Set, constructor);
    end,
    constructor = function (self)

    end,
    union = function (self, b)

    end,
    union = function (self)

    end,
    union = function (self)

    end,
}